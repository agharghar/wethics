{% extends 'base.html.twig' %}

{% block title %}EfficaciteCO index{% endblock %}

{% block body %}
    <h1>EfficaciteCO index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Risque</th>
                <th>Benefice</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for efficacite_c_o in efficacite_c_os %}
            <tr>
                <td>{{ efficacite_c_o.id }}</td>
                <td>{{ efficacite_c_o.risque }}</td>
                <td>{{ efficacite_c_o.benefice }}</td>
                <td>
                    <a href="{{ path('efficacite_c_o_show', {'id': efficacite_c_o.id}) }}">show</a>
                    <a href="{{ path('efficacite_c_o_edit', {'id': efficacite_c_o.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('efficacite_c_o_new') }}">Create new</a>
{% endblock %}
