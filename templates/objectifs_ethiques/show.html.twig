{% extends 'base.html.twig' %}

{% block title %}ObjectifsEthiques{% endblock %}

{% block body %}
    <h1>ObjectifsEthiques</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ objectifs_ethique.id }}</td>
            </tr>
            <tr>
                <th>Objectif_ethique</th>
                <td>{{ objectifs_ethique.objectifEthique }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('objectifs_ethiques_index') }}">back to list</a>

    <a href="{{ path('objectifs_ethiques_edit', {'id': objectifs_ethique.id}) }}">edit</a>

    {{ include('objectifs_ethiques/_delete_form.html.twig') }}
{% endblock %}
