{% extends 'base.html.twig' %}

{% block title %}MethodesEvaluation index{% endblock %}

{% block body %}
    <h1>MethodesEvaluation index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Methode_evaluation</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for methodes_evaluation in methodes_evaluations %}
            <tr>
                <td>{{ methodes_evaluation.id }}</td>
                <td>{{ methodes_evaluation.methodeEvaluation }}</td>
                <td>
                    <a href="{{ path('methodes_evaluation_show', {'id': methodes_evaluation.id}) }}">show</a>
                    <a href="{{ path('methodes_evaluation_edit', {'id': methodes_evaluation.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('methodes_evaluation_new') }}">Create new</a>
{% endblock %}
