<form method="post" action="{{ path('rating_user_delete', {'id': rating_user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rating_user.id) }}">
    <button class="btn">Delete</button>
</form>
