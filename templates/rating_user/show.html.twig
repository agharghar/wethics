{% extends 'base.html.twig' %}

{% block title %}RatingUser{% endblock %}

{% block body %}
    <h1>RatingUser</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rating_user.id }}</td>
            </tr>
            <tr>
                <th>Risque</th>
                <td>{{ rating_user.risque }}</td>
            </tr>
            <tr>
                <th>Benefice</th>
                <td>{{ rating_user.benefice }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rating_user_index') }}">back to list</a>

    <a href="{{ path('rating_user_edit', {'id': rating_user.id}) }}">edit</a>

    {{ include('rating_user/_delete_form.html.twig') }}
{% endblock %}
