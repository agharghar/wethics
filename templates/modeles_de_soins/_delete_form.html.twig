<form method="post" action="{{ path('modeles_de_soins_delete', {'id': modeles_de_soin.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ modeles_de_soin.id) }}">
    <button class="btn">Delete</button>
</form>
